# 复制为 .env.local 覆盖默认值
# 后端 admin API 基地址。缺省：开发=http://localhost:5000，生产=https://api.sandtable.club
VITE_API_BASE=http://localhost:5000

# 后端 /admin/* 尚未实现时，置 true 使用内置 mock 数据，前端可独立跑通
VITE_USE_MOCK=true
